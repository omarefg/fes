import { StyleSheet } from 'react-native'

const Styles = StyleSheet.create({
    container: {
        padding: 8
    },
    text: {
        fontSize: 24,
        marginBottom: 10
    }
})

export default Styles
